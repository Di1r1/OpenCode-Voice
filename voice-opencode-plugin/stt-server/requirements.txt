# OpenCode Voice — © 2026 Di1r1 · MIT · https://github.com/Di1r1/OpenCode-Voice
# Runtime dependencies for the STT server.
#
# flask        — HTTP API (required)
# faster-whisper — CPU backend. Imported lazily: a GPU-only install
#                  (whisper.cpp) works without it, so it is optional at runtime.
flask==3.1.3
faster-whisper==1.2.1
